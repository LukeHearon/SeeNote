# Screenshots and figures

The `.qmd` pages reference images here (`media/image1.png`, etc.). These were
not present in the original `local/protocol` export — drop the corresponding
screenshots into this folder using the same filenames and they'll render. Until
then, the image references will show as broken on the rendered site.
